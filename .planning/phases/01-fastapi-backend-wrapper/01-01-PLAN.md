---
phase: 01-fastapi-backend-wrapper
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - requirements.txt
  - api/__init__.py
  - api/main.py
  - api/deps.py
  - api/routers/__init__.py
  - api/services/__init__.py
  - tests/conftest.py
  - tests/test_api_smoke.py
autonomous: true
requirements:
  - BACKEND-01

must_haves:
  truths:
    - "uvicorn api.main:app starts cleanly from repo root on port 8000"
    - "All 4 endpoints (/api/recommend, /api/current-prices, /api/explain, /api/etfs/{ticker}) are registered before any router file is written"
    - "App startup loads the cached ML artifacts via src.inference._get_artifacts and stashes them on app.state for downstream services"
    - "Missing artifacts cause a fail-fast RuntimeError naming `python pipeline.py` as the regeneration command"
    - "Tests can boot a TestClient without ML artifacts on disk (graceful skip)"
  artifacts:
    - path: "api/main.py"
      provides: "FastAPI app factory with lifespan that loads models + ETF universe; includes 4 routers"
      min_lines: 40
    - path: "api/deps.py"
      provides: "Dependency-injection helpers (get_models, get_etf_universe) reading from app.state"
      min_lines: 15
    - path: "tests/conftest.py"
      provides: "Shared pytest fixtures (client, sample_profile, ARTIFACTS_PRESENT skip-gate)"
      min_lines: 35
    - path: "tests/test_api_smoke.py"
      provides: "Smoke test verifying all 4 routes register"
      min_lines: 15
    - path: "requirements.txt"
      provides: "Pinned fastapi>=0.115 + uvicorn[standard]>=0.30 entries"
      contains: "fastapi"
  key_links:
    - from: "api/main.py"
      to: "src.inference._get_artifacts"
      via: "lifespan startup hook"
      pattern: "_get_artifacts"
    - from: "api/main.py"
      to: "api.routers.{recommend,prices,explain,etfs}"
      via: "app.include_router stubs"
      pattern: "include_router"
    - from: "tests/conftest.py"
      to: "api.main.app"
      via: "TestClient fixture"
      pattern: "from api.main import app"
---

<objective>
Wave 0 — install the FastAPI runtime, scaffold the `api/` package, wire the lifespan to load existing ML artifacts via `src/inference._get_artifacts`, and seed test infrastructure so subsequent wave 1 plans can land their routers and tests with zero scaffolding cost.

Purpose: Every subsequent plan imports from `api.main`, `api.deps`, and `tests/conftest.py`. Without this wave the rest of the phase has no shared substrate. This plan also establishes the additivity invariant by **not** touching `pipeline.py`, `src/inference.py`, or `app.py`.

Output: A boot-able but mostly-empty FastAPI app whose `/docs` shows zero routes (routers attach in wave 1), plus a green smoke test confirming the app instantiates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-fastapi-backend-wrapper/01-CONTEXT.md
@.planning/phases/01-fastapi-backend-wrapper/01-RESEARCH.md
@.planning/phases/01-fastapi-backend-wrapper/01-PATTERNS.md
@.planning/phases/01-fastapi-backend-wrapper/01-VALIDATION.md
@src/inference.py
@requirements.txt

<interfaces>
<!-- Key contracts the executor needs to land Wave 0. Extracted from the codebase. -->
<!-- Do NOT re-explore the repo for these — they're authoritative as of 2026-05-19. -->

From src/inference.py (lines 42-44, 106-112):
```
MODEL_DIR    = 'models'
TRAINING_DIR = 'data/training'
ETF_FILE     = 'data/raw/combined_etf_with_morning_star.csv'

_ARTIFACTS = None
def _get_artifacts():
    global _ARTIFACTS
    if _ARTIFACTS is None:
        _ARTIFACTS = _load_artifacts()
    return _ARTIFACTS
```

Existing artifact files on disk (verified):
- models/risk_profile_classifier.pkl
- models/allocation_regressor.pkl
- models/etf_count_regressor.pkl
- models/label_map.json
- data/training/preprocessor.pkl
- data/raw/combined_etf_with_morning_star.csv

From tests/test_data_validation.py (lines 32-46) — canonical valid investor profile to mirror in conftest:
```
{
    'Age': 30, 'Gender': 'Male', 'Education': 'Bachelor',
    'MaritalStatus': 'Single', 'HouseholdSize': 1,
    'Income': 80000, 'InvestmentGoal': 'Retirement',
    'InvestmentHorizon': 30, 'InvestmentCapital': 50000,
    'RiskTolerance': 'Medium', 'FinancialInvolvement': 'Medium',
    'ExperienceYears': 5,
}
```

Router stubs to register (real routers land in plans 02/03/04/05):
- api.routers.recommend.router  (prefix=/api)
- api.routers.prices.router     (prefix=/api)
- api.routers.explain.router    (prefix=/api)
- api.routers.etfs.router       (prefix=/api)

Wave 0 ships ONLY empty `APIRouter()` stubs in each `api/routers/*.py` so `include_router` works. Real handlers land in subsequent plans which OVERWRITE these stubs.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install FastAPI + uvicorn and pin them in requirements.txt</name>
  <files>requirements.txt</files>
  <read_first>
    - requirements.txt (full file — 17 lines, current state has streamlit/pytest/joblib/yfinance/openai/pandas etc.)
    - .planning/phases/01-fastapi-backend-wrapper/01-PATTERNS.md (line 33 — additive entries `fastapi>=0.115`, `uvicorn[standard]>=0.30`)
    - .planning/phases/01-fastapi-backend-wrapper/01-RESEARCH.md (lines 147-161 — installation + verified PyPI versions)
  </read_first>
  <action>
    Run `venv/bin/pip install "fastapi>=0.115" "uvicorn[standard]>=0.30"`. Then APPEND (do not rewrite) two pinned entries to `requirements.txt` after the existing `pytest>=7.0` line: `fastapi>=0.115` and `uvicorn[standard]>=0.30`. Leave all existing entries (pandas, numpy, scikit-learn, joblib, yfinance, requests, finnhub-python, python-dotenv, xgboost, streamlit, plotly, openai, pytest, matplotlib, seaborn, jupyter, notebook) untouched. Do NOT run `pip freeze > requirements.txt` — that would clobber the existing constraint style.
  </action>
  <verify>
    <automated>venv/bin/python -c "import fastapi, uvicorn; print(fastapi.__version__, uvicorn.__version__)"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c '^fastapi' requirements.txt` returns 1
    - Source assertion: `grep -c '^uvicorn' requirements.txt` returns 1
    - Source assertion: `grep -v '^#' requirements.txt | wc -l` returns at least 19 (was 17, added 2)
    - CLI: `venv/bin/python -c "import fastapi" && venv/bin/python -c "import uvicorn"` exits 0
  </acceptance_criteria>
  <done>fastapi and uvicorn import without error from the project venv, both packages are pinned in requirements.txt, and no existing dependency lines were modified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Scaffold api/ package with lifespan-loaded models and router stubs</name>
  <files>api/__init__.py, api/main.py, api/deps.py, api/routers/__init__.py, api/routers/recommend.py, api/routers/prices.py, api/routers/explain.py, api/routers/etfs.py, api/services/__init__.py</files>
  <read_first>
    - src/inference.py (lines 42-112 — MODEL_DIR/TRAINING_DIR/ETF_FILE constants and `_get_artifacts` cached-singleton idiom that lifespan will warm)
    - .planning/phases/01-fastapi-backend-wrapper/01-PATTERNS.md (lines 281-342 — lifespan composition with `_get_artifacts` + fail-fast pattern from `pipeline.py` lines 76-81)
    - .planning/phases/01-fastapi-backend-wrapper/01-RESEARCH.md (lines 314-385 — Pattern 1: FastAPI lifespan; verified pattern that calls `_get_artifacts()` inside `@asynccontextmanager`)
    - pipeline.py (lines 76-81 — fail-fast-on-missing-file template to mirror in the lifespan error message)
  </read_first>
  <behavior>
    - Test 1: `from api.main import app` succeeds without error when ML artifacts exist.
    - Test 2: `app.routes` contains paths for `/api/recommend`, `/api/current-prices`, `/api/explain`, and `/api/etfs/{ticker}` after wave 1 lands (wave 0 routers are EMPTY but `include_router` must be wired).
    - Test 3: When artifacts are missing, `TestClient(app)` raises `RuntimeError` with a message containing `python pipeline.py`.
  </behavior>
  <action>
    Create `api/__init__.py` (empty), `api/services/__init__.py` (empty), `api/routers/__init__.py` (empty).

    Create `api/routers/recommend.py`, `api/routers/prices.py`, `api/routers/explain.py`, `api/routers/etfs.py` — each EMPTY STUB containing only `from fastapi import APIRouter` and `router = APIRouter(prefix="/api", tags=["<endpoint>"])`. No handlers. Subsequent plans will OVERWRITE these stubs. The `tags` value: `"recommend"`, `"prices"`, `"explain"`, `"etfs"` respectively.

    Create `api/main.py`:
    - Imports: `from contextlib import asynccontextmanager`, `from fastapi import FastAPI`, `from api.routers import recommend, prices, explain, etfs`.
    - Define `@asynccontextmanager async def lifespan(app: FastAPI)`. Inside:
        - `from src.inference import _get_artifacts`
        - Try-block: `artifacts = _get_artifacts()`; on `(FileNotFoundError, OSError) as e` re-raise as `RuntimeError("Cannot start API: {e}. Run `python pipeline.py` to regenerate models.") from e`.
        - Stash on `app.state`: `app.state.classifier`, `app.state.alloc_regressor`, `app.state.count_regressor`, `app.state.preprocessor`, `app.state.label_info`, `app.state.etf_features` — pulling these keys from the dict returned by `_get_artifacts()`. Use whatever keys `_load_artifacts` actually populates; the Read step above tells you the exact names from `src/inference.py`.
        - `yield` (no shutdown cleanup needed).
    - Instantiate `app = FastAPI(title="AI Financial Advisor API", version="0.1.0", lifespan=lifespan)`.
    - Register all 4 routers: `app.include_router(recommend.router)`, `app.include_router(prices.router)`, `app.include_router(explain.router)`, `app.include_router(etfs.router)`.
    - Do NOT add CORS middleware (locked decision: no CORS in Phase 1).

    Create `api/deps.py`:
    - `from fastapi import Request` import.
    - Define `get_models(request: Request) -> dict` returning a dict with keys `classifier`, `alloc_regressor`, `count_regressor`, `preprocessor`, `label_info` sourced from `request.app.state`.
    - Define `get_etf_features(request: Request)` returning `request.app.state.etf_features`.

    Do NOT modify `src/inference.py`, `pipeline.py`, `app.py`, or any file under `src/` in this task.
  </action>
  <verify>
    <automated>venv/bin/python -c "from api.main import app; print(len(app.routes))"</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: `from api.main import app` returns a `FastAPI` instance.
    - Source assertion: `grep -c "include_router" api/main.py` returns 4.
    - Source assertion: `grep -c "from src.inference import _get_artifacts" api/main.py` returns 1.
    - Source assertion: `grep -c "python pipeline.py" api/main.py` returns 1 (the fail-fast message).
    - Source assertion: `ls api/routers/{recommend,prices,explain,etfs}.py` lists 4 files.
    - CLI: `venv/bin/python -c "from api.main import app; assert app.title == 'AI Financial Advisor API'"` exits 0.
  </acceptance_criteria>
  <done>Importing `api.main` succeeds, the app has lifespan wired to `src.inference._get_artifacts`, all 4 routers are registered as empty stubs, `api/deps.py` exposes app-state accessors, and `src/inference.py` / `pipeline.py` / `app.py` are byte-identical to their pre-task state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create tests/conftest.py shared fixtures + tests/test_api_smoke.py</name>
  <files>tests/conftest.py, tests/test_api_smoke.py</files>
  <read_first>
    - tests/test_data_validation.py (lines 32-46 — canonical `_make_good_investor_row` fixture builder to mirror in conftest)
    - .planning/phases/01-fastapi-backend-wrapper/01-PATTERNS.md (lines 380-447 — test style, fixture pattern, ARTIFACTS_PRESENT skip-gate)
    - .planning/phases/01-fastapi-backend-wrapper/01-VALIDATION.md (lines 75-105 — exact `conftest.py` shape with `ARTIFACTS_PRESENT` constant, session-scoped `client` fixture, `sample_profile` fixture)
    - api/main.py (just created — `app` symbol that conftest imports)
  </read_first>
  <behavior>
    - Test 1: `pytest tests/test_api_smoke.py -x` passes when artifacts exist.
    - Test 2: When artifacts are missing, the `client` fixture skips the test (does not fail/error).
    - Test 3: The smoke test asserts all 4 endpoint paths (`/api/recommend`, `/api/current-prices`, `/api/explain`, `/api/etfs/{ticker}`) are registered on `app.routes`.
  </behavior>
  <action>
    Create `tests/conftest.py`:
    - Imports: `pytest`, `from pathlib import Path`.
    - Module-level constant `ARTIFACTS_PRESENT = all([Path(p).exists() for p in [...]])` listing the 6 paths: `models/risk_profile_classifier.pkl`, `models/allocation_regressor.pkl`, `models/etf_count_regressor.pkl`, `models/label_map.json`, `data/training/preprocessor.pkl`, `data/raw/combined_etf_with_morning_star.csv`.
    - Session-scoped fixture `client()`: if not `ARTIFACTS_PRESENT`, `pytest.skip("ML artifacts missing — run `python pipeline.py` to regenerate")`. Else `from fastapi.testclient import TestClient; from api.main import app; with TestClient(app) as c: yield c`.
    - Fixture `sample_profile()` (function-scoped, no `scope=`): returns the 12-field dict exactly mirroring `tests/test_data_validation.py::_make_good_investor_row` with no kwargs — same keys and values.
    - Helper `_make_good_investor_row(**overrides) -> dict` exposed at module level (so individual test files can call it).

    Create `tests/test_api_smoke.py`:
    - Single test `test_all_four_endpoints_registered(client)`:
        - `paths = {r.path for r in client.app.routes}`
        - `assert "/api/recommend" in paths`
        - `assert "/api/current-prices" in paths`
        - `assert "/api/explain" in paths`
        - `assert "/api/etfs/{ticker}" in paths`
    - At this stage (wave 0) the routers are empty stubs so these paths DO NOT yet exist on the app. The smoke test is therefore expected to FAIL after Wave 0 and PASS after Wave 1. Mark the test with `@pytest.mark.xfail(reason="Routes register in wave 1", strict=False)` so wave 0 verification is green. Subsequent plans (02/03/04/05) MUST remove the xfail marker as they add their respective endpoint.

    Do not introduce mocking libraries; the smoke test is pure introspection.
  </action>
  <verify>
    <automated>venv/bin/pytest tests/test_api_smoke.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - CLI: `venv/bin/pytest tests/test_api_smoke.py -x -q` exits 0 (xfail counts as pass in wave 0).
    - Source assertion: `grep -c "ARTIFACTS_PRESENT" tests/conftest.py` returns at least 2 (definition + skip gate).
    - Source assertion: `grep -c "sample_profile" tests/conftest.py` returns at least 1.
    - Behavior: Running `venv/bin/pytest tests/test_api_smoke.py -x -q` in an environment where ML artifacts are deleted reports "skipped" not "errored".
  </acceptance_criteria>
  <done>`tests/conftest.py` exposes `client` (with ARTIFACTS skip-gate) and `sample_profile` fixtures, `tests/test_api_smoke.py` exists with the xfail-marked introspection test, and the full test suite (existing 24 tests + 1 new) runs cleanly via `venv/bin/pytest -x -q`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none in Phase 1 wave 0) | No user input crosses any boundary; this is pure local scaffolding. Trust boundaries appear in wave 1 routers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-SC | Tampering | `pip install fastapi`, `pip install uvicorn` | mitigate | Per `.planning/phases/01-fastapi-backend-wrapper/01-RESEARCH.md` Package Legitimacy Audit (lines 165-178), both packages are `[ASSUMED OK]` blue-chip libraries with multi-year history and >50M monthly downloads from the encode/tiangolo orgs. No checkpoint required per the researcher's explicit finding. |
| T-01-01-DOS | Denial of Service | Lifespan startup with missing ML artifacts | mitigate | Lifespan catches `FileNotFoundError`/`OSError` and re-raises as `RuntimeError` with the actionable `python pipeline.py` regen command, preventing partial-init silent failures. |
| T-01-01-INFO | Information Disclosure | FastAPI default `/docs` Swagger UI | accept | Local-only dev surface (no CORS, no auth, Next.js is the only intended caller per CONTEXT.md). Spec explicitly leaves auth deferred. |
</threat_model>

<verification>
- `venv/bin/python -c "from api.main import app; print(app.title)"` prints "AI Financial Advisor API".
- `venv/bin/pytest tests/test_api_smoke.py -x -q` exits 0 (xfail counted as pass).
- `venv/bin/pytest tests/test_data_validation.py -x -q` STILL passes (additivity — existing 24 tests untouched).
- `grep -c "fastapi" requirements.txt` returns at least 1.
- `git status` shows no modifications to `src/inference.py`, `pipeline.py`, or `app.py`.
</verification>

<success_criteria>
- requirements.txt has `fastapi>=0.115` and `uvicorn[standard]>=0.30` appended without disturbing other entries.
- `api/` package exists with `main.py`, `deps.py`, `routers/` (4 empty stubs), `services/` (empty).
- `api/main.py` lifespan loads ML artifacts via `src.inference._get_artifacts` and fails fast with `python pipeline.py` message.
- `tests/conftest.py` exposes `client` (session-scoped, ARTIFACTS_PRESENT-gated) and `sample_profile` fixtures.
- `tests/test_api_smoke.py` exists with xfail-marked route-introspection test.
- No file in `src/`, no `pipeline.py`, no `app.py` was modified.
</success_criteria>

<output>
Create `.planning/phases/01-fastapi-backend-wrapper/01-01-SUMMARY.md` when done.
</output>

## Conflict Detection Report

Generated by gsd-doc-synthesizer from 1 classified source: docs/MIGRATION-SPEC.md (PRD, manifest-override, precedence=2, locked=false).
Mode: new. No prior `.planning/` decisions or requirements to cross-check.

---

### BLOCKERS (0)

None. With only a single source document and no existing locked decisions to cross-check, no LOCKED-vs-LOCKED contradictions or hard blockers are possible.

---

### WARNINGS (0)

None. With a single PRD there are no competing acceptance variants between sources. Within the spec, requirements for each of the 4 features and 5 phases are stated once and consistently.

---

### INFO (6)

[INFO] Single-source synthesis
  Source: docs/MIGRATION-SPEC.md
  Note: Only one classified document exists in this ingest run. All synthesized intel derives from this single PRD. Any future ADRs/SPECs that ingest later may contradict assertions here — at that point they will be re-evaluated under precedence rules.
  → No action required.

[INFO] PRD asserts architectural "decisions" without an ADR
  Source: docs/MIGRATION-SPEC.md § "System Architecture", § "Data Model", § "Database Requirements", § "LLM Layer Architecture"
  Note: Several items the spec asserts as fixed (3-tier topology, 12-field profile schema, 4-table DB, exactly-2 LLM tools, baseline-vs-current state isolation, Vercel AI SDK) read like ADR-level decisions but live inside a PRD (precedence 2, not locked). They have been written into decisions.md as "locked-ish" with explicit provenance.
  → If the user wants any of these protected against future PRD churn, promote them to standalone ADRs with `locked: true` and re-ingest.

[INFO] `adjust_allocation` delta semantics under-specified for non-equity/bond classes
  Source: docs/MIGRATION-SPEC.md § "Tool definitions / adjust_allocation"
  Note: The spec gives `equity_delta` and `bond_delta` as the example inputs, but does not enumerate the full set of allowable delta keys (e.g., cash, alternatives, international, etc.). It also does not specify the renormalization algorithm when multiple deltas conflict, or behavior if a delta would push a class below 0 or above 1 before renormalization.
  → Captured under CON-adjust-allocation-tool-contract. User/roadmapper should clarify the asset-class taxonomy and clamp/renormalization order before implementation.

[INFO] `update_investor_profile` "wipes qualitative overrides" — provenance of `current_allocation` is implicit
  Source: docs/MIGRATION-SPEC.md § "Tool definitions / update_investor_profile" + § "State isolation rules"
  Note: After a factual update, `ml_result` is replaced and the spec says qualitative overrides are wiped — so presumably `current_allocation` is reset to equal the new `ml_result`. This is implied but not stated verbatim. Captured as inferred behavior in CON-update-investor-profile-tool-contract and CON-state-isolation-invariants.
  → Roadmapper should confirm with user, then make this explicit in the implementation.

[INFO] Repo layout (monorepo vs split repos) is not declared
  Source: docs/MIGRATION-SPEC.md § "Execution Plan / Phase 3"
  Note: Phase 1 says FastAPI is built "inside the origin repo"; Phase 3 says the Next.js frontend is initialized "in `frontend/`". This is consistent with a monorepo (frontend/ as a subdirectory of the Python repo) but the spec does not say so explicitly. No conflict, but a downstream decision.
  → Roadmapper should flag this as an open question or first-week decision.

[INFO] No explicit auth/RLS or multi-user isolation policy stated
  Source: docs/MIGRATION-SPEC.md § "Database Requirements"
  Note: `profiles` is linked to `auth.users` (implying Supabase auth + per-user data), but the spec does not specify Row-Level Security policies, ownership constraints on `portfolios`/`portfolio_history`/`chat_messages`, or auth-flow requirements (sign-up/sign-in UX). For a production-grade Supabase app this is normally required.
  → Roadmapper may want to surface as an implicit phase-2 sub-requirement or a separate auth ADR.
